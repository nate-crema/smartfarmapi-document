// test data

const session = {
    name: "tester",
    id: "test",
    department: "Admin",
    grade: "2",
    level: "headmaster"
}

$(document).ready(function() {
    $("body").css("width", $(window).width());
    $("body").css("height", $(window).height());
})